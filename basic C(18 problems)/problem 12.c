//Write a C program to input a number and find square root of the given number.
#include<stdio.h>
int main()
{
    float a,root;
    printf("enter a=");
    scanf("%f",&a);

    root= sqrt(a);
    printf("root=%.2f",root);



    return 0;
}
